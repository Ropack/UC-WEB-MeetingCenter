
<%= simple_form_for [:admin, @admin_building] do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :title %>
    <%= f.input :code %>
  </div>

  <div class="form-actions">
    <%= f.button :magic_submit %>
  </div>
<% end %>
